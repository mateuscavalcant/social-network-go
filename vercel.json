{
    "version": 2,
    "builds": [
        { "src": "handler/*.go", "use": "@vercel/go" }
    ],
    "routes": [
        { "src": "/public/(.*)", "dest": "/public/$1" },
        { "src": "/(.*)", "dest": "/handler/$1" }
    ]
}
